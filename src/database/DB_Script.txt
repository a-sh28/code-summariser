create database code_summarizer;
use code_summarizer;

create table admin_data (admin_id int not null unique, admin_username varchar(255) not null unique, admin_password varchar(255) not null unique, primary key (admin_id));

create table user_data (user_id int not null unique, user_username varchar(255) not null unique, user_password varchar(255) not null unique, user_category varchar(255) not null, primary key (user_id));

create table code_summary (code_id int not null unique, summary text not null, user_id int not null,  naturalness int not null, usefulness int not null, consistency int not null, feedback text not null, primary key (code_id), foreign key (user_id) references user_data(user_id));